{
  "name": "mazdoorplaylist",
  "version": "1.0.0",
  "description": "Indian Mistri Playlist with live listener counts",
  "scripts": {
    "postinstall": "cd backend && npm install",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}